// Déclaration des tableaux contenant les listes des mots proposés à l'utilisateur
const listeMots = ["Bonjour", "Salut", "Coucou"]
const listePhrases = ["Bonjour, comment allez-vous ?", "Salut, ça va ?", "Coucou ça va ?"]
const listBoutons = document.querySelectorAll("button")

/**
 * Initialiser un dépôt Git et envoyer les modifications : Si tu veux transformer le répertoire en dépôt Git, tu peux initialiser un nouveau dépôt Git en exécutant :
git init
git add .
git commit -m "Save Js tools"
Pour envoyer les modifications sur mon GitHub
First you need to generate a new token.
My account - Setting - Developer Setting - Personal Access Token - Generate New Token
After you generate the token,
git remote set-url origin https://TOKEN@github.com/MaximeFlorent/JavascriptTools.git
git push --force origin main
 */